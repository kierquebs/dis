!-- REDEMPTION TABLE --!
ALTER TABLE `redemption` ADD `REFUND_ID` INT NOT NULL AFTER `PAYMENT_MODE`, ADD `PA_ID` INT NOT NULL AFTER `REFUND_ID`, ADD `PA_TEMPID` INT NOT NULL AFTER `PA_ID`, ADD INDEX (`REFUND_ID`), ADD INDEX (`PA_ID`), ADD INDEX (`PA_TEMPID`);

!-- RECON TABLE --!
ALTER TABLE `reconcilation` ADD `REDEEM_TBL_ID` INT NOT NULL AFTER `payment_mode`, ADD `STAGE` VARCHAR(50) NOT NULL AFTER `REDEEM_TBL_ID`, ADD `PA_TEMPID` INT NOT NULL AFTER `STAGE`, ADD INDEX (`REDEEM_TBL_ID`), ADD INDEX (`PA_TEMPID`);
!-- REFUND TABLE --!
ALTER TABLE `refund` ADD `REDEEM_TBL_ID` INT NOT NULL AFTER `PA_ID`, ADD `RECON_TBL_ID` INT NOT NULL AFTER `REDEEM_TBL_ID`, ADD `PA_TEMPID` INT NOT NULL AFTER `RECON_TBL_ID`, ADD INDEX (`REDEEM_TBL_ID`), ADD INDEX (`RECON_TBL_ID`), ADD INDEX (`PA_TEMPID`);

!-- BRANCH_MERCHANT TABLE --!
ALTER TABLE branch_merchant ADD UNIQUE KEY mid_bid (MERCHANT_ID,BRANCH_ID);

!-- payment_cutoff TABLE --!
ALTER TABLE `payment_cutoff` ADD `DigitalSettlementType` VARCHAR(250) NOT NULL AFTER `SPECIFIC_DATE`;
